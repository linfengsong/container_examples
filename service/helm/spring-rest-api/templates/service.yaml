apiVersion: v1
kind: Service
metadata:
  annotations:
    fabric8.io/ingress.annotations: |-
      ingress.kubernetes.io/force-ssl-redirect: false
      kubernetes.io/ingress.class: {{ .Values.springrestapi.service.ingressClass }}
  labels:
    expose: "true"
    app: spring-rest-api
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: {{ .Release.Name | quote }}
    heritage: {{ .Release.Service | quote }}
    version: 1.0.0
  name: spring-rest-api
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: spring-rest-api
  sessionAffinity: None
  type: ClusterIP
