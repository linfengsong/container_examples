apiVersion: v1
kind: Service
metadata:
  name: loadbalance-route
  namespace: default
  labels:
    app: swag
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: swag