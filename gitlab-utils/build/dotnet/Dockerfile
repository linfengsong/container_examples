ARG DOCKER_IMAGE_PATH
ARG DOCKER_IMAGE_TAG
ARG PRISMA_EMAIL
FROM $DOCKER_IMAGE_PATH:$DOCKER_IMAGE_TAG
FROM $DOCKER_IMAGE_PATH
LABEL "prisma_email_alert"="$PRISMA_EMAIL"
ARG PUBLISH_DIR=/publish
WORKDIR /app
COPY $PUBLISH_DIR .
ENTRYPOINT ["dotnet", "/app/aspnetapp.dll"]
