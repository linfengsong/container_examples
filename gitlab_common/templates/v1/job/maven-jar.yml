variables:
  MAVEN_OPTS: ""

maven-jar-job:
  stage: build
  script:
    - |
      echo "build maven build start"
      mvn $MAVEN_OPTS -Dmaven.test.skip=true clean package
      cp target/*.jar target/app.jar
      echo "build maven build finish"
  artifacts:
    paths:
      - target/app.jar
