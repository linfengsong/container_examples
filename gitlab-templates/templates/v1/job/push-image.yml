
docker-job:
  stage: docker
  script: 
  - |
    echo "start push image"
    
    mkdir -p $GITLAB_UTILS_PATH
    tar xf gitlab_utils.tar -C $GITLAB_UTILS_PATH
    export PATH=$GITLAB_UTILS_PATH/tool:$PATH
      
    envName=dev
    srcPath=$(pwd)
    outputPath=$srcPath/output
    source $GITLAB_UTILS_PATH/script/config.sh $srcPath $envName  
    $GITLAB_UTILS_PATH/script/push_image.sh "$outputPath"
    
    echo "finish push image"
 