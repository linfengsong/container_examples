
docker-job:
  stage: docker
  image: $BUILDER_IMAGE_UBI8PLUS
  script: 
  - |
    echo "start push image"
    
    srcPath=$(pwd)
    outputPath=$srcPath/output
    source $GITLAB_UTILS_PATH/ci/script/init_ci.sh
    $GITLAB_UTILS_PATH/ci/script/push_image.sh "$outputPath"
    
    echo "finish push image"
 