
test-image-job:
  stage: test
  image: $BUILDER_IMAGE_NAME
  script:
    - |
      echo "start unit tests"
      
      srcPath=$(pwd)
      $GITLAB_UTILS_PATH/script/action.sh -ci test_image $CI_APP $srcPath

      echo "finish unit tests"
