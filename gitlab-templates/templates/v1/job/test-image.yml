
test-image-job:
  stage: test
  image: $BUILDER_IMAGE_NAME
  script:
    - |
      echo "start unit tests"
      
      srcPath=$(pwd)
      source $GITLAB_UTILS_PATH/ci/script/init_ci.sh
      $GITLAB_UTILS_PATH/ci/script/test_image.sh $srcPath

      echo "finish unit tests"
