
.bcp_container_deploy_job:
  image: $BUILDER_IMAGE_UBI8PLUS
  script:
  - |
    echo "start deploy bcp container: $CONTAINER_NAME"
    
    mkdir -p $GITLAB_UTILS_PATH
    tar xf gitlab_utils.tar -C $GITLAB_UTILS_PATH
    
    srcPath=$(pwd)
    $GITLAB_UTILS_PATH/script/config.sh $srcPath $ENV_NAME $CONTAINER_NAME
    source $GITLAB_UTILS_PATH/script/init.sh
    $GITLAB_UTILS_PATH/script/container_deploy.sh
    
    echo "finish deploy bcp container: $CONTAINER_NAME"
    